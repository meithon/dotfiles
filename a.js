

let a = 1;

const list = []
list.map((asdf) => {
  console.log(asdf)
})

const users = await ctx
  .prisma
  .query
  .users().
    
  })
const asd = asd

  () => {

}
function name(arguments: T)<T> {

  const name = await a.constructor.name
}

